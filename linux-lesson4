# show command to display contents of all files in /root/ops-test and output them to a single file

# the other files in /root/ops-test should not be impacted

# make the new file immutable (show command)

# Use tar to create a tarball of all the files in /root/ops-test, it should be compressed
